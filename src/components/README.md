# components/

Reusable, presentational UI building blocks (buttons, cards, inputs, nav items).
Keep them small, prop-driven, and free of page/route-specific logic.

- `ui/` — low-level primitives (shadcn-style base components).
- Feature-specific composite components live in subfolders per feature.